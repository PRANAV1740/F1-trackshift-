# Demo Mode

**Status: not yet populated.** Demo mode lands in Phase 29, after the full
pipeline (ingestion → normalization → state → intelligence layers → events
→ strategy → prediction → pit wall/HQ) is working end-to-end.

This file will document how to run the deterministic demo scenario
(normal race → tyre degradation → opponent approach → VSC → pit
opportunity → strategy reassessment → PIT decision → compound selection →
position prediction change → outcome), its `scenario_id`/`seed`, and how to
verify every displayed value traces back to the actual engine rather than
being hard-coded.
