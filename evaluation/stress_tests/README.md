# evaluation/stress_tests

**Status:** not yet implemented.

Extreme-condition tests beyond the unit-level noise tests in `tests/`:
sustained noisy telemetry, sustained missing packets, rapid SC/VSC
toggling, rapid degradation, etc., run against the full pipeline rather
than one stage in isolation (problem prompt section 30).
