# models/tyre_model

**Status:** not yet implemented (Phase 5).

Any fitted (as opposed to purely physics/statistical) component of tyre
degradation estimation used by `backend/tyre`. Per problem prompt section
29: start with physics + statistics; only add gradient boosting (or a
neural net) here if it measures out as better than the simpler baseline,
and document input features, target, training method, validation, metrics,
and limitations for whatever ends up here.
