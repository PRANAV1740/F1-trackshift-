# models/position_model

**Status:** not yet implemented (Phase 9).

Any fitted component of the finishing-position distribution used by
`backend/prediction`. Same documentation requirement as `models/tyre_model`.
