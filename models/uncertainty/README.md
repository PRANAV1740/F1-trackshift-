# models/uncertainty

**Status:** not yet implemented.

Shared uncertainty-estimation utilities used across `models/tyre_model`,
`models/pace_model`, and `models/position_model`, so every intelligence
layer reports confidence in a consistent way rather than each module
inventing its own notion of confidence.
