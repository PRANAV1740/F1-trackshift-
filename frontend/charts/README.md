# frontend/charts

**Status:** not yet implemented.

Chart components (pace trace, degradation curve, position-probability bars,
gap-over-time, etc.) shared across `frontend/pitwall` and `frontend/hq`.
Every number rendered here must trace back to a real backend response --
no hard-coded example data left in a shipped component (problem prompt
section 34).
