# frontend/components

**Status:** not yet implemented.

Shared UI components used by both `frontend/pitwall` and `frontend/hq`
(decision card, confidence badge, alert banner, etc.), so the two
dashboards stay visually consistent without duplicating markup.
